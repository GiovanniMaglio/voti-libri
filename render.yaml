services:
  - type: web
    name: recensito
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: sh -c "gunicorn app:app --bind 0.0.0.0:${PORT} --workers 2 --log-level debug"
    envVars:
      - key: DATABASE_URL
        value: postgresql://postgres:FisicaStatale2024@rujiwukbcoexouvsuyhg.supabase.co:5432/postgres
